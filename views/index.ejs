<DOCTYPE html>

<html>
    <head>
        <meta http-equiv="content-type" content="text/html"; charset="UTF-8">
        <title><%= title %></title>
        <link rel="stylesheet"
        href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" crossorigin="anonymous">
    </head>
    <body class="container">
        <header>
            <h1 class="display-4"><%= title %></h1>
        </header>
        <input type="submit" value="履歴情報の追加" href="/add">
        <div role="main">
            <table class="table">
                <th>history_id</th>
                <th>date</th>
                <th>user_id</th>
                <th>user_name</th>
                <th>book_name</th>
                <th>book_price</th>
                <% for(var i in content) {%>
                    <tr>
                        <% var obj = content[i]; %>
                        <td><%= obj.history_id %></td>
                        <td><%= obj.date %></td>
                        <td><%= obj.user_id %></td>
                        <td><%= obj.user_name %></td>
                        <td><%= obj.book_name %></td>
                        <td><%= obj.book_price %></td>
                    </tr>
                <% } %>
            </table>
        </div>
    </body>
</html>